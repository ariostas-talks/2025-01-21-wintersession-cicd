name: CIExample

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4
      - name: second step
        run: |
          echo 'Hello world'
          mkdir testdir
          ls -la
